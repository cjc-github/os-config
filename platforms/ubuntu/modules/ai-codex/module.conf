# module.conf —— ai-codex 模块元数据
# 由 runner.sh 解析（NAME / DEPS / NEEDS_SUDO），供 setup.sh --list 显示

NAME=codex
DESC=安装 OpenAI Codex CLI（@openai/codex@固定版本）；包名与版本在 config/versions.env 修改
DEPS=nodejs
NEEDS_SUDO=0
