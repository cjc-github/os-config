# module.conf —— ai-claude 模块元数据
# 由 runner.sh 解析（NAME / DEPS / NEEDS_SUDO），供 setup.sh --list 显示

NAME=claude
DESC=安装 Claude Code CLI（@anthropic-ai/claude-code@固定版本）；版本号在 config/versions.env 修改
DEPS=nodejs
NEEDS_SUDO=0
