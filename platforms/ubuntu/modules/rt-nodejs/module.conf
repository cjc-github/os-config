# module.conf —— rt-nodejs 模块元数据
# 由 runner.sh 解析（NAME / DEPS / NEEDS_SUDO），供 setup.sh --list 显示

NAME=nodejs
DESC=通过 nvm 安装 node LTS 并配置 npm 镜像；nvm/node 版本与镜像在 config/versions.env 修改
DEPS=base
NEEDS_SUDO=0
