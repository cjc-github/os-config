# module.conf —— ai-opencode 模块元数据
# 由 runner.sh 解析（NAME / DEPS / NEEDS_SUDO），供 setup.sh --list 显示

NAME=opencode
DESC=安装 OpenCode CLI（opencode-ai@固定版本，注意包名是 opencode-ai 不是 opencode）；包名与版本在 config/versions.env 修改
DEPS=nodejs
NEEDS_SUDO=0
