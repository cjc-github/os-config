# module.conf —— rt-log 模块元数据
# 由 runner.sh 解析（NAME / DEPS / NEEDS_SUDO），供 setup.sh --list 显示

NAME=log
DESC=安装 lnav/bat/logrotate + journald 持久化限容 + logrotate 调优 + oslogs 别名；容量与保留周数在 config/user.env 修改
DEPS=base
NEEDS_SUDO=1
