# module.conf —— mirrors 模块元数据（由 runner.sh 解析）
# 每行 NAME=VALUE 格式；DEPS 可用逗号指定多个前置模块（其 install 先跑）
NAME=mirrors
DESC=配置国内镜像源（apt deb822 + npm registry 可覆盖），加速下载；Ubuntu 24.04 使用 deb822 格式 ubuntu.sources，非传统 sources.list
DEPS=
NEEDS_SUDO=1
