# module.conf —— net-git 模块元数据
# 由 runner.sh 解析（NAME / DEPS / NEEDS_SUDO），供 setup.sh --list 显示

NAME=git
DESC=安装 git 并配置 user.name/email 与 http/https 代理；用户参数在 config/user.env 修改
DEPS=base
NEEDS_SUDO=1
