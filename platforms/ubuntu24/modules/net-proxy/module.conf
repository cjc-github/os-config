# module.conf —— net-proxy 模块元数据
# 由 runner.sh 解析（NAME / DEPS / NEEDS_SUDO），供 setup.sh --list 显示

NAME=proxy
DESC=把 HTTP_PROXY/HTTPS_PROXY/NO_PROXY 持久化写入 ~/.bashrc（不安装代理软件）；代理地址在 config/user.env 修改
DEPS=base
NEEDS_SUDO=0
