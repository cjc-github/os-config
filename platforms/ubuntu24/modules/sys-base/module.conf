# module.conf —— sys-base 模块元数据
# 由 runner.sh 解析（NAME / DEPS / NEEDS_SUDO），供 setup.sh --list 显示

NAME=base
DESC=系统基础：apt update/upgrade 并安装 curl/wget/ca-certificates/gnupg/unzip 基础工具包（无版本参数）
DEPS=mirrors
NEEDS_SUDO=1
