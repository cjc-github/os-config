# module.conf —— net-ssh 模块元数据
# 由 runner.sh 解析（NAME / DEPS / NEEDS_SUDO），供 setup.sh --list 显示

NAME=ssh
DESC=安装 openssh-client/server 并非交互生成 SSH 密钥（ed25519/rsa）；密钥类型与 passphrase 在 config/user.env 修改
DEPS=base
NEEDS_SUDO=1
